services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: coinwise
      POSTGRES_USER: coinwise
      POSTGRES_PASSWORD: coinwise
    ports: ["5432:5432"]
    volumes: ["postgres-data:/var/lib/postgresql/data"]
volumes:
  postgres-data:
